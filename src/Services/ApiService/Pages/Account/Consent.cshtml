@page
@model ApiService.Pages.Account.ConsentModel
@{
    ViewData["Title"] = "授权确认";
}

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - IAM</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .consent-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 520px;
            padding: 40px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .header h1 {
            color: #212529;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }
        
        .header p {
            color: #6c757d;
            font-size: 14px;
        }
        
        .app-info {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 24px;
            text-align: center;
        }
        
        .app-icon {
            width: 64px;
            height: 64px;
            background: #667eea;
            border-radius: 12px;
            margin: 0 auto 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
        }
        
        .app-name {
            font-size: 18px;
            font-weight: 600;
            color: #212529;
            margin-bottom: 4px;
        }
        
        .app-description {
            font-size: 14px;
            color: #6c757d;
        }
        
        .user-info {
            background: #e7f3ff;
            border-left: 4px solid #0066cc;
            padding: 12px 16px;
            margin-bottom: 24px;
            border-radius: 4px;
        }
        
        .user-info p {
            font-size: 14px;
            color: #004085;
        }
        
        .scopes-section {
            margin-bottom: 24px;
        }
        
        .scopes-section h2 {
            font-size: 16px;
            font-weight: 600;
            color: #212529;
            margin-bottom: 16px;
        }
        
        .scope-item {
            display: flex;
            align-items: flex-start;
            padding: 12px;
            margin-bottom: 8px;
            background: #f8f9fa;
            border-radius: 6px;
        }
        
        .scope-icon {
            width: 24px;
            height: 24px;
            background: #28a745;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 14px;
            margin-right: 12px;
            flex-shrink: 0;
        }
        
        .scope-content {
            flex: 1;
        }
        
        .scope-name {
            font-size: 14px;
            font-weight: 500;
            color: #212529;
            margin-bottom: 2px;
        }
        
        .scope-description {
            font-size: 13px;
            color: #6c757d;
        }
        
        .required-badge {
            background: #ffc107;
            color: #000;
            font-size: 11px;
            padding: 2px 8px;
            border-radius: 3px;
            font-weight: 600;
            margin-left: 8px;
        }
        
        .actions {
            display: flex;
            gap: 12px;
            margin-bottom: 20px;
        }
        
        .btn {
            flex: 1;
            padding: 14px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .btn-allow {
            background: #28a745;
            color: white;
        }
        
        .btn-allow:hover {
            background: #218838;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(40, 167, 69, 0.3);
        }
        
        .btn-deny {
            background: #6c757d;
            color: white;
        }
        
        .btn-deny:hover {
            background: #5a6268;
        }
        
        .privacy-notice {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 12px 16px;
            border-radius: 6px;
            margin-bottom: 20px;
        }
        
        .privacy-notice p {
            font-size: 13px;
            color: #856404;
            line-height: 1.5;
        }
        
        .checkbox-group {
            margin-bottom: 20px;
        }
        
        .checkbox-group label {
            display: flex;
            align-items: center;
            font-size: 14px;
            color: #495057;
            cursor: pointer;
        }
        
        .checkbox-group input {
            margin-right: 8px;
            width: 18px;
            height: 18px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="consent-container">
        <div class="header">
            <h1>授权确认</h1>
            <p>请审查并确认授权</p>
        </div>

        <div class="app-info">
            <div class="app-icon">📱</div>
            <div class="app-name">@Model.ClientName</div>
            <div class="app-description">@Model.ClientDescription</div>
        </div>

        <div class="user-info">
            <p>您将以 <strong>@Model.UserName</strong> 的身份授权此应用</p>
        </div>

        <div class="scopes-section">
            <h2>该应用请求以下权限：</h2>
            @foreach (var scope in Model.RequestedScopes)
            {
                <div class="scope-item">
                    <div class="scope-icon">✓</div>
                    <div class="scope-content">
                        <div class="scope-name">
                            @scope.DisplayName
                            @if (scope.Required)
                            {
                                <span class="required-badge">必需</span>
                            }
                        </div>
                        <div class="scope-description">@scope.Description</div>
                    </div>
                </div>
            }
        </div>

        <div class="privacy-notice">
            <p>💡 授权后，该应用将能够访问您选择的信息。您可以随时在账号设置中撤销授权。</p>
        </div>

        <form method="post">
            <input type="hidden" asp-for="ReturnUrl" />
            <input type="hidden" asp-for="ClientId" />
            <input type="hidden" asp-for="Scope" />
            <input type="hidden" asp-for="State" />
            <input type="hidden" asp-for="Nonce" />
            <input type="hidden" asp-for="CodeChallenge" />
            <input type="hidden" asp-for="CodeChallengeMethod" />
            <input type="hidden" asp-for="RedirectUri" />
            <input type="hidden" asp-for="ResponseType" />

            <div class="checkbox-group">
                <label>
                    <input asp-for="RememberConsent" type="checkbox" />
                    记住我的选择，不再询问
                </label>
            </div>

            <div class="actions">
                <button type="submit" name="action" value="allow" class="btn btn-allow">
                    ✓ 允许访问
                </button>
                <button type="submit" name="action" value="deny" class="btn btn-deny">
                    ✗ 拒绝
                </button>
            </div>
        </form>
    </div>
</body>
</html>
